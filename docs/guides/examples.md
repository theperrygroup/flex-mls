# Examples

Example scripts live in the `examples/` directory:

- `direct_token.py` for bearer-token authentication
- `oidc_flow.py` for Spark OpenID Connect redirect setup
- `property_expansions.py` for `$expand` usage
- `replication_polling.py` for bounded replication windows
