# Guides

The guides focus on the Spark-specific behavior the library wraps:

- OData query parameters such as `$select`, `$filter`, and `$expand`
- replication-safe paging with `@odata.nextLink`
- bounded `ModificationTimestamp` polling windows
- syncing `Property`, `Member`, and `Office` data together
