# API Reference

The API reference is generated from Google-style docstrings using
`mkdocstrings`.

Use the pages in this section to inspect the facade client, authentication
helpers, and resource-specific clients.
